# /Users/Bas/.config/tmuxinator/ps_core.yml

name: ps_core
root: ~/Developer/papershift/core

windows:
  - vim:
      layout: main-horizontal-mirrored
      panes:
        -
        - v
  - server:
      layout: even-horizontal
      panes:
        - docker-compose up --menu=false
        - lazydocker
  - zsh:
  - lazygit: lazygit
  - db: # TODO: Fix dadbod bootstrap
