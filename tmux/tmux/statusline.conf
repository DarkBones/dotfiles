set -g status on
set -g status-right-length 100
set -g status-left-length 100
set -g status-interval 5

set -g status-left ""
set -g status-right ""

# Git diff summary
set -ag status-right " #[fg=#a6e3a1]#($DOTFILES_REPO/tmux/tmux/scripts/git-diff-summary #{pane_current_path})"

# Catppuccin segments (application, session)
set -ag status-right " #{E:@catppuccin_status_application}"
set -ag status-right "#{E:@catppuccin_status_session}"

# User@host segment
set -ag status-right "#[fg=#b4befe]#[fg=#11111b,bg=#b4befe]👤 #[fg=#cdd6f4,bg=#313244] #{client_user}@#h #[fg=#313244]"

# SSH indicator
set -ag status-right "#{?client_ssh, #[fg=#89b4fa]🔐 ,}"

# Synchronized panes indicator
set -ag status-right "#{?synchronize-panes, #[fg=#f38ba8]SYNC ,}"
